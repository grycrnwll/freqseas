% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R, R/collection.R
\name{decision}
\alias{decision}
\alias{decision.seas_test}
\alias{decision.seas_sa}
\alias{decision.default}
\alias{decision.seas_collection}
\title{Extract the seasonality decision}
\usage{
decision(object, ...)

\method{decision}{seas_test}(object, ...)

\method{decision}{seas_sa}(object, ...)

\method{decision}{default}(object, ...)

\method{decision}{seas_collection}(object, ...)
}
\arguments{
\item{object}{A fitted freqseas object (\code{seas_test}, \code{seas_sa}, or a
\code{seas_collection}).}

\item{...}{Passed on to methods; unused by the methods defined here.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{seasonal} (logical), \code{p}
(numeric), \code{spec} (character, \code{"line"}/\code{"band"}), and \code{alpha}
(numeric); one row for a single object, or one row per key (plus the
key columns) for a \code{seas_collection}.
}
\description{
A one-row (single object) or one-row-per-key (\code{seas_collection}) tibble
summarizing the detection decision: whether the series is judged
seasonal (\code{evt$p < alpha}), the detection p-value, the specification
label (\code{"line"}/\code{"band"}), and the test level \code{alpha}.
}
\examples{
tst <- list(
  x = 1:12, input_class = "numeric", N = 12, P = 1, M = 3, alpha = 0.05,
  evt = list(statistic = 2.1, p = 0.01, critical = 1.8),
  spec = list(label = "line", shoulder_p = 0.2, phase_R = 0.9),
  whitener = list(d = 0, d_reason = "not near-integrated", p = 1,
                   ar = 0.4, bic_path = c(-10, -12, -11))
)
class(tst) <- "seas_test"
decision(tst)

}
