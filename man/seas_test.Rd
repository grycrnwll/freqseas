% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seas_test.R
\name{seas_test}
\alias{seas_test}
\alias{seas_test.default}
\alias{seas_test.ts}
\title{Frequency-domain seasonality test (detect + specify)}
\usage{
seas_test(x, ...)

\method{seas_test}{default}(
  x,
  frequency = NULL,
  P = 1,
  M = NULL,
  alpha = 0.05,
  d = c("auto", "none", "first"),
  ar_max = 3,
  band = c("full", "between_seasonal_extremes"),
  whiten_exclusion = c("guard", "bins"),
  whiten_guard = 3L,
  ...
)

\method{seas_test}{ts}(
  x,
  P = 1,
  M = NULL,
  alpha = 0.05,
  d = c("auto", "none", "first"),
  ar_max = 3,
  band = c("full", "between_seasonal_extremes"),
  whiten_exclusion = c("guard", "bins"),
  whiten_guard = 3L,
  ...
)
}
\arguments{
\item{x}{A numeric vector (with \code{frequency}) or a \code{stats::ts} object.}

\item{...}{Passed to methods.}

\item{frequency}{Observations per year (e.g. 4 quarterly, 12 monthly).
Required for a bare numeric \code{x}; inferred for \code{ts}.}

\item{P}{Positive integer fundamental-per-year multiplier. Default \code{1}.}

\item{M}{Number of equal-width frequency bins on \verb{(0, pi]}. Default \code{NULL},
in which case the offset-free \code{\link[=suggest_M]{suggest_M()}} chooses it (recorded in the
result).}

\item{alpha}{Test level for the detection and shoulder tests. Default \code{0.05}.}

\item{d}{Differencing rule passed to the whitener: \code{"auto"} (difference once
iff near-integrated), \code{"none"}, or \code{"first"}. Default \code{"auto"}.}

\item{ar_max}{Maximum AR order for the de-biased-Whittle BIC search. Default
\code{3}.}

\item{band}{Bin-restriction rule: \code{"full"} (default) or
\code{"between_seasonal_extremes"}.}

\item{whiten_exclusion}{Ordinate-exclusion geometry for the whitening fit,
passed to \code{\link[=fs_whiten]{fs_whiten()}}: \code{"guard"} (default) or \code{"bins"}. See
\code{\link[=fs_whiten]{fs_whiten()}}'s Details for the size/power tradeoff and the measured
numbers behind the default.}

\item{whiten_guard}{Guard half-width (Fourier steps) for
\code{whiten_exclusion = "guard"}. Default \code{2}.}
}
\value{
An object of class \code{"seas_test"}: a list with fields \code{x},
\code{input_class}, \code{N}, \code{P}, \code{M}, \code{M_selection}, \code{partition} (grid, breaks,
\code{J1}/\code{J0}/\code{H}/\code{S} and their full-DFT counterparts, \code{M1}/\code{M0}, \code{omega_G}),
\code{whitener} (the \code{\link[=fs_whiten]{fs_whiten()}} record: \code{d}, \code{d_reason}, \code{mu}, \code{p}, \code{ar},
\code{bic_path}, ...), \code{e} (whitened residuals), \code{E} (their DFT), \code{pgram},
\code{donor_pool}, \code{donor_level}, \code{tau_hat}, \code{evt} (\code{statistic}, \code{p},
\code{critical}), \code{spec} (\code{label} = \code{"line"}/\code{"band"}, \code{shoulder_p},
\code{phase_R}, \code{harmonic_table} = a per-harmonic excess tibble with columns
\code{omega}/\code{label}/\code{excess}/\code{elevated}, and \code{comb_note} = a character flag,
\code{NA} unless the excess is elevated at only a strict subset of harmonics),
\code{decision} (logical), \code{alpha}, \code{ar_max}, and \code{call}.
}
\description{
Runs the freqseas Stage 0 + A + B pipeline on a single series: M0-Whittle
whitening (Stage 0), an extreme-value detection test for excess mass at the
seasonal frequencies against a white-noise null (Stage A), and a line-versus-
band specification test built from a shoulder EVT and a descriptive
block-phase resultant (Stage B). Nothing is adjusted here; a not-seasonal
series still returns a full object with the decision recorded.

\code{seas_test()} is an S3 generic. The \code{.default} method takes a bare numeric
vector and a stated \code{frequency}; the \code{.ts} method infers \code{frequency} from
the series. A \code{.tbl_ts} (keyed tsibble) method is provided by the collection
layer.
}
\examples{
# Quarterly series with a seasonal band -> detected, specified "band".
set.seed(1)
n <- 120L; Phi <- 0.8
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
x <- stats::ts(z[401:(n + 400)], frequency = 4)
tst <- seas_test(x)
tst$decision
tst$spec$label

}
