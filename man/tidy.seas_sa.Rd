% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-broom.R
\name{tidy.seas_sa}
\alias{tidy.seas_sa}
\title{Tidy a \code{seas_sa} object}
\usage{
\method{tidy}{seas_sa}(x, ...)
}
\arguments{
\item{x}{A \code{seas_sa} object.}

\item{...}{Passed on to \code{\link[=tidy.seas_test]{tidy.seas_test()}}.}
}
\value{
See \code{\link[=tidy.seas_test]{tidy.seas_test()}}.
}
\description{
Delegates to \code{\link[=tidy.seas_test]{tidy.seas_test()}} on the embedded test (\code{x$test}): the
adjustment stage does not add per-harmonic structure of its own.
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
adj <- seas_ssi(tst, phase_rule = "minimum")
tidy(adj)

}
