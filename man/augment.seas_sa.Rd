% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-broom.R
\name{augment.seas_sa}
\alias{augment.seas_sa}
\title{Augment a \code{seas_sa} object}
\usage{
\method{augment}{seas_sa}(x, ...)
}
\arguments{
\item{x}{A \code{seas_sa} object.}

\item{...}{Ignored.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} (or \code{\link[tsibble:tsibble]{tsibble::tsibble()}} for \code{tbl_ts} input)
with columns \code{index} (or the original tsibble's index column name), \code{x}
(the original series), \code{adjusted}, \code{seasonal}.
}
\description{
Observation-level output: the original series, the adjusted series, and
the removed seasonal component, one row per observation. For a \code{ts}- or
numeric-input adjustment, the index column is the (fractional-year, for
\code{ts}) time value; for a \code{tbl_ts}-input adjustment, the result is itself a
tsibble indexed on the original tsibble's index column.
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
adj <- seas_ssi(tst, phase_rule = "minimum")
augment(adj)

}
