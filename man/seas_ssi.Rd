% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seas_ssi.R
\name{seas_ssi}
\alias{seas_ssi}
\alias{seas_ssi.seas_test}
\title{Seasonal adjustment from a \code{seas_test} (declared-phase SSI surgery)}
\usage{
seas_ssi(object, ...)

\method{seas_ssi}{seas_test}(object, phase_rule, donor_quantile = 0.9, ...)
}
\arguments{
\item{object}{A \code{seas_test} object (or, via the collection layer, a
\code{seas_collection}).}

\item{...}{Passed to methods.}

\item{phase_rule}{Required. One of \code{"minimum"} or \code{"zero"} (see Description).
There is deliberately no default; omitting it raises a teaching error that
points to \code{vignette("two-worlds")}.}

\item{donor_quantile}{Upper cutoff on the M0 periodogram distribution when
selecting the donor (reference) ordinates. Default \code{0.9}.}
}
\value{
An object of class \code{"seas_sa"}: a list with \code{test} (the embedded
\code{seas_test}), \code{adjusted} (input class preserved), \code{seasonal}
(\code{x - adjusted}), \code{Ghat} (gain applied), \code{theta} (phase applied),
\code{phase_rule}, \code{post_evt} (detection p-value of the re-test on the adjusted
series -- a self-check), \code{spec} (\code{"band"}, \code{"line"}, or \code{"none"}), and
\code{call}.
}
\description{
\code{seas_ssi()} performs Stage C of the workflow: given a completed
\code{\link[=seas_test]{seas_test()}}, it removes the estimated seasonal filter and returns a
\code{seas_sa}. The removal happens on the DFT of the whitened residuals and is
inverted through the whitener's recoloring, so seasonality can only re-enter
through the cleaned innovations (never via fitted values).

\code{phase_rule} is a required IDENTIFICATION declaration, not a tuning knob:
\itemize{
\item \code{"minimum"} -- filter world (shared innovations): removal is
deconvolution, dividing by the gain AND de-rotating by the implied
minimum phase.
\item \code{"zero"} -- components world (independent seasonal): removal is
Wiener-style shrinkage, dividing by the gain only (zero phase).
}
It is always echoed in print/summary tagged \verb{[declared identification]}.

If the test decided the series is not seasonal, the input is returned
unchanged (with a loud message) wrapped in a \code{seas_sa} with unit gain and
zero phase. Otherwise the band branch estimates the gain from the donor pool
and applies the phase rule; the line (deterministic-sinusoid) branch resets
the exact-harmonic ordinates to the donor level and leaves phase alone.
}
\examples{
set.seed(1)
n <- 120L; Phi <- 0.8
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
x   <- stats::ts(z[401:(n + 400)], frequency = 4)
tst <- seas_test(x)
adj <- seas_ssi(tst, phase_rule = "minimum")
adj$post_evt          # detection p-value after adjustment (self-check)

}
