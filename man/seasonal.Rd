% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R, R/collection.R
\name{seasonal}
\alias{seasonal}
\alias{seasonal.seas_sa}
\alias{seasonal.seas_test}
\alias{seasonal.default}
\alias{seasonal.seas_collection}
\title{Extract the removed seasonal component}
\usage{
seasonal(object, ...)

\method{seasonal}{seas_sa}(object, ...)

\method{seasonal}{seas_test}(object, ...)

\method{seasonal}{default}(object, ...)

\method{seasonal}{seas_collection}(object, ...)
}
\arguments{
\item{object}{A fitted freqseas object (\code{seas_sa}, or a \code{seas_collection}
produced by \code{\link[=seas_adjust]{seas_adjust()}}/\code{seas_ssi()}).}

\item{...}{Passed on to methods; unused by the methods defined here.}
}
\value{
The seasonal component, in the same class as the original input
for a single-series object, or a tibble (key columns + list-column) for
a collection.
}
\description{
The component removed by the adjustment: for a \code{seas_sa}, its \code{seasonal}
field (\code{x - adjusted}, same class as the original input); for a
\code{seas_collection} of adjustments, a tibble of key columns plus a list
column of per-key seasonal components.
}
\examples{
fit <- list(
  test = list(x = 1:12, input_class = "numeric", alpha = 0.05,
              evt = list(p = 0.01), spec = list(label = "line")),
  adjusted = 1:12 - 0.1, seasonal = rep(0.1, 12),
  phase_rule = "minimum", post_evt = 0.6
)
class(fit) <- "seas_sa"
seasonal(fit)

}
