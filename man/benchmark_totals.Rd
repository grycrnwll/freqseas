% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_totals}
\alias{benchmark_totals}
\title{Benchmark a series to complete-period totals of another}
\usage{
benchmark_totals(adjusted, original, period = c("year"), frequency = NULL)
}
\arguments{
\item{adjusted}{The series to rescale: a \code{\link[stats:ts]{stats::ts()}} (frequency inferred)
or a bare numeric vector (supply \code{frequency}).}

\item{original}{The series providing the target complete-period totals; same
length as \code{adjusted}. A \code{ts} or numeric vector.}

\item{period}{The period whose totals are constrained. Currently only
\code{"year"} (annual totals), the default.}

\item{frequency}{Observations per year. Required when \code{adjusted} is a bare
numeric vector; ignored when \code{adjusted} is a \code{ts} (its own frequency is
used). If both \code{adjusted} and \code{original} are \code{ts}, their frequencies must
agree.}
}
\value{
The benchmarked series, in the same class as \code{adjusted} (a \code{ts} with
\code{adjusted}'s \code{tsp} if \code{adjusted} was a \code{ts}, else a plain numeric vector).
It carries a \code{"benchmark"} attribute: a list with \code{period}, \code{frequency},
\code{scale_factors} (one multiplicative factor per complete window), and
\code{n_tail_unadjusted} (the number of trailing observations left unchanged).
}
\description{
Proportionally rescales \code{adjusted} so that each complete period's total
matches the corresponding period's total in \code{original}. This is the
NIPA-style benchmarking constraint that \strong{annual totals are invariant to
seasonal adjustment}: a seasonally adjusted series should redistribute
activity \emph{within} a year without changing the year's total.

The rescaling is proportional (multiplicative), a port of the
\code{yearly_total_matching()} helper: within each complete period, every value
of \code{adjusted} is multiplied by the single factor
\code{sum(original_window) / sum(adjusted_window)}, so the rescaled window's total
equals the original window's total exactly.
}
\details{
Windowing is \strong{positional}: consecutive, non-overlapping windows of one
period's length are taken from index 1 onward, with no calendar awareness
(matching the port). For \code{period = "year"} the window length is the annual
frequency (\code{4} for quarterly, \code{12} for monthly). A series that begins on the
first sub-period of a year -- e.g. a quarterly \code{ts} starting in Q1 -- has
positional windows that coincide with calendar years.

If the length of the series is not a whole number of periods, the trailing
\strong{incomplete} window is left unadjusted and a warning is issued (there is
no complete total to match it to). There is no leading partial window,
because windows start at index 1.
}
\section{Errors}{

\code{benchmark_totals()} errors informatively when \code{adjusted} and \code{original}
differ in length, when either contains non-finite values, when a bare
numeric \code{adjusted} is given with no \code{frequency}, when two \code{ts} inputs have
mismatched frequencies, when the series is shorter than one complete period,
or when a complete window of \code{adjusted} sums to zero (proportional
benchmarking is then undefined -- there is no factor that makes a zero total
match a nonzero one).
}

\examples{
# Constrain a seasonal adjustment so annual totals match the original's:
orig    <- ssi_examples$x[[1]]                    # filter-world series (ts)
adj     <- seas_adjust(orig, phase_rule = "minimum")
matched <- benchmark_totals(adjusted(adj), orig)  # ts in, ts out

# Annual totals of `matched` now equal those of `orig` exactly:
N  <- stats::frequency(orig)
yo <- colSums(matrix(as.numeric(orig),    nrow = N))
ym <- colSums(matrix(as.numeric(matched), nrow = N))
max(abs(ym - yo))                                 # ~0

}
