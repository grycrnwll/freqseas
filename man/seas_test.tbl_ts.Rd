% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{seas_test.tbl_ts}
\alias{seas_test.tbl_ts}
\title{Test a tsibble for seasonality, mapping over keys}
\usage{
\method{seas_test}{tbl_ts}(x, frequency = NULL, ...)
}
\arguments{
\item{x}{A \code{tbl_ts} (tsibble) with a regular index and exactly one
measured variable.}

\item{frequency}{Ignored for \code{tbl_ts} input; the annual frequency is
always inferred from the index (see \code{fs_extract()}). Present only so
the method's signature matches the generic's.}

\item{...}{Passed on to \code{seas_test.default()} for each series (e.g. \code{P},
\code{M}, \code{alpha}, \code{d}, \code{ar_max}, \code{band}).}
}
\value{
A \code{seas_test} (if \code{x} is unkeyed, i.e. has a single series) or a
\code{seas_collection} of type \code{"test"} (if \code{x} has one or more key columns).
}
\description{
The \code{tbl_ts} method for \code{seas_test()}: extracts one numeric series per key
(via \code{fs_extract()}), runs the single-series test
(\code{seas_test.default()}) on each, and returns either a bare \code{seas_test}
(an unkeyed tsibble holding a single series) or a \code{seas_collection} (a
keyed tsibble, one fit per key).
}
\examples{
\dontrun{
library(tsibble)
seas_test(some_keyed_tsibble)
}

}
