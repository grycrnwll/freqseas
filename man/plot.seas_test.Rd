% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{plot.seas_test}
\alias{plot.seas_test}
\title{Plot a \code{seas_test} object}
\usage{
\method{plot}{seas_test}(x, type = c("periodogram"), ...)
}
\arguments{
\item{x}{A \code{seas_test} object.}

\item{type}{Plot type. Only \code{"periodogram"} is currently implemented.}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
The standardized periodogram of the whitened series against
\eqn{\omega/\pi}: seasonal-bin ordinates highlighted in blue, donor
ordinates in gray, the EVT rejection threshold as a dashed reference line
(\code{max(I[J0]) + evt$critical}, the level a seasonal-bin ordinate would
need to exceed to trigger detection at \code{alpha}), and dotted vertical
lines at the equal-width bin boundaries.
}
\examples{
\donttest{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
plot(tst)
}

}
