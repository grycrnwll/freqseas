% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{autoplot.seas_sa}
\alias{autoplot.seas_sa}
\title{Plot a \code{seas_sa} object with ggplot2}
\usage{
\method{autoplot}{seas_sa}(object, type = c("series", "periodogram"), ...)
}
\arguments{
\item{object}{A \code{seas_sa} object.}

\item{type}{Plot type: \code{"series"} (default) or \code{"periodogram"}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
The ggplot2 equivalent of \code{\link[=plot.seas_sa]{plot.seas_sa()}}: same content (\code{type = "series"}: original vs adjusted overlaid, seasonal component beneath, as
facets; \code{type = "periodogram"}: before/after standardized periodograms),
\code{ggplot2::theme_minimal()} chrome. Requires the ggplot2 package (Suggests).
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(2026)
  n <- 120L; Phi <- 0.9
  e <- stats::rnorm(n + 400)
  z <- numeric(n + 400)
  for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
    0.5 * Phi * z[t - 5] + e[t]
  tst <- seas_test(z[401:(n + 400)], frequency = 4)
  adj <- seas_ssi(tst, phase_rule = "minimum")
  ggplot2::autoplot(adj)
}
}

}
