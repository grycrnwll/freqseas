% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-broom.R
\name{glance.seas_test}
\alias{glance.seas_test}
\title{Glance at a \code{seas_test} object}
\usage{
\method{glance}{seas_test}(x, ...)
}
\arguments{
\item{x}{A \code{seas_test} object.}

\item{...}{Ignored.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{decision} (logical),
\code{p.value}, \code{spec}, \code{M}, \code{d}, \code{ar_order}.
}
\description{
A one-row model-level summary: the detection decision, p-value,
specification label, bin count, differencing order, and AR order.
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
glance(tst)

}
