% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{autoplot.seas_test}
\alias{autoplot.seas_test}
\title{Plot a \code{seas_test} object with ggplot2}
\usage{
\method{autoplot}{seas_test}(object, type = c("periodogram"), ...)
}
\arguments{
\item{object}{A \code{seas_test} object.}

\item{type}{Plot type. Only \code{"periodogram"} is currently implemented.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
The ggplot2 equivalent of \code{\link[=plot.seas_test]{plot.seas_test()}}: same content (standardized
periodogram, seasonal-bin ordinates in blue, donor ordinates in gray, the
EVT rejection threshold, dotted bin boundaries), \code{ggplot2::theme_minimal()}
chrome. Requires the ggplot2 package (Suggests).
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(2026)
  n <- 120L; Phi <- 0.9
  e <- stats::rnorm(n + 400)
  z <- numeric(n + 400)
  for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
    0.5 * Phi * z[t - 5] + e[t]
  tst <- seas_test(z[401:(n + 400)], frequency = 4)
  ggplot2::autoplot(tst)
}
}

}
