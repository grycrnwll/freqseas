% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{seas_adjust.tbl_ts}
\alias{seas_adjust.tbl_ts}
\title{Adjust a tsibble in one call, mapping over keys}
\usage{
\method{seas_adjust}{tbl_ts}(x, ..., phase_rule = "minimum")
}
\arguments{
\item{x}{A \code{tbl_ts} (tsibble) with a regular index and exactly one
measured variable.}

\item{...}{Passed on to \code{seas_test()} (e.g. \code{P}, \code{M}, \code{alpha}, \code{d},
\code{ar_max}, \code{band}).}

\item{phase_rule}{\code{"minimum"} (default) or \code{"zero"}; the declared
identification passed to \code{seas_ssi()}.}
}
\value{
A \code{seas_sa} (if \code{x} is unkeyed) or a \code{seas_collection} of type
\code{"sa"} (if \code{x} has one or more key columns).
}
\description{
The \code{tbl_ts} method for \code{seas_adjust()}: the one-call wrapper, \code{seas_ssi( seas_test(x, ...), phase_rule = phase_rule)}, dispatched through
\code{\link[=seas_test.tbl_ts]{seas_test.tbl_ts()}} (and, for keyed input, \code{\link[=seas_ssi.seas_collection]{seas_ssi.seas_collection()}},
which re-clothes each key's \code{adjusted}/\code{seasonal} itself). For an unkeyed
\code{x}, \code{seas_ssi()} dispatches straight to \code{seas_ssi.seas_test()} with no
method of ours in between, so this method re-clothes the bare result
itself via \code{fs_reclothe_sa()} (see the design note at the top of this
file).
}
\examples{
\dontrun{
library(tsibble)
seas_adjust(some_keyed_tsibble, phase_rule = "minimum")
}

}
