% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition.R
\name{suggest_M}
\alias{suggest_M}
\title{Suggest a bin count M for the seasonal/nonseasonal partition}
\usage{
suggest_M(
  P,
  N,
  n_obs = NULL,
  target_rho = 0.25,
  rho_bounds = c(0.15, 0.35),
  min_ordinates_per_bin = NULL,
  exclude_nyquist = TRUE,
  search_min = NULL,
  search_max = NULL,
  offset_grid = 0,
  boundary_tol = .Machine$double.eps^0.5,
  return_candidates = FALSE
)
}
\arguments{
\item{P}{Nonempty vector of positive integers (fundamental-period
multipliers).}

\item{N}{Single positive integer, observations per year.}

\item{n_obs}{Optional series length; when supplied, enables the average
ordinates-per-bin diagnostic and the optional occupancy constraint.}

\item{target_rho}{Target seasonal-bin share \code{K_M / M}. Default \code{0.25}.}

\item{rho_bounds}{Length-2 increasing acceptable range for \code{K_M / M}.
Default \code{c(0.15, 0.35)}.}

\item{min_ordinates_per_bin}{Optional minimum average Fourier ordinates per
bin (requires \code{n_obs}).}

\item{exclude_nyquist}{Passed to \code{omega_seasonal()}. Default \code{TRUE} (the
detection convention).}

\item{search_min, search_max}{Optional integer bounds for the \code{M} search;
sensible defaults are derived from \code{H} and \code{target_rho}.}

\item{offset_grid}{Grid of partition offsets \code{alpha / (pi / M)} in \verb{[0, 1)}
tried per \code{M}; the first boundary-safe offset is kept. Default \code{0}
(offset-free; see Details).}

\item{boundary_tol}{Numerical tolerance for detecting boundary hits. Default
\code{.Machine$double.eps^0.5}.}

\item{return_candidates}{If \code{TRUE}, attach the full candidate table.}
}
\value{
A list with the suggested \code{M}, the number of seasonal frequencies
\code{H}, the number of seasonal bins \code{K_M}, the realized share \code{rho_M}, the
partition offset \code{alpha} (radians) and \code{offset_u}, and diagnostics
(\code{M0}, \code{boundary_safe}, \code{avg_ordinates_per_bin}, \code{n_obs}, \code{J_n}, \code{omega},
and optionally \code{candidates}).
}
\description{
Choose a fixed number of frequency bins \code{M} for an equal-width partition of
\verb{(0, pi)} so that every distinct seasonal frequency in \code{omega_seasonal(P, N)}
occupies its own bin (one harmonic per bin), no seasonal frequency lands on a
bin boundary, and the seasonal-bin share \code{K_M / M} sits close to \code{target_rho}
(within \code{rho_bounds} when possible).
}
\details{
The package's binning is \strong{offset-free}: bins are the fixed partition
\verb{((m - 1) pi / M, m pi / M]}. Accordingly the default \code{offset_grid = 0} only
considers that partition. The reference implementation shipped
\code{offset_grid = c(0, 0.5)}, which can select a boundary-avoiding partition
shifted by half a bin; such offsets are \strong{not realizable} by the package's
binning (they would require an \code{alpha}-shifted \code{mbin_partition}), so they are
documented but off by default. A nonzero offset can therefore return an \code{M}
with a nonzero \code{offset_u}/\code{alpha} that the rest of the pipeline cannot honor.
}
\examples{
# Offset-free default, quarterly (N = 4), fundamental P = 1, n = 60:
res <- suggest_M(P = 1, N = 4, n_obs = 60)
res$M      # 5
res$rho_M  # 0.2

# The reference default offset grid can realize M = 4 with a half-bin
# offset, but that partition is not honored by the package's binning:
res2 <- suggest_M(P = 1, N = 4, n_obs = 60, offset_grid = c(0, 0.5))
res2$M         # 4
res2$offset_u  # 0.5

}
