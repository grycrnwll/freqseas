% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seas_adjust.R
\name{seas_adjust}
\alias{seas_adjust}
\alias{seas_adjust.default}
\alias{seas_adjust.ts}
\title{One-call seasonal adjustment}
\usage{
seas_adjust(x, phase_rule = "minimum", donor_quantile = 0.9, ...)

\method{seas_adjust}{default}(x, phase_rule = "minimum", donor_quantile = 0.9, ...)

\method{seas_adjust}{ts}(x, phase_rule = "minimum", donor_quantile = 0.9, ...)
}
\arguments{
\item{x}{A numeric vector (with \code{frequency}) or a \code{stats::ts} object.}

\item{phase_rule}{Identification rule, \code{"minimum"} (default) or \code{"zero"}.
See \code{\link[=seas_ssi]{seas_ssi()}}.}

\item{donor_quantile}{Upper cutoff on the M0 periodogram distribution for the
donor pool. Default \code{0.9}.}

\item{...}{Passed to \code{\link[=seas_test]{seas_test()}} (e.g. \code{frequency}, \code{P}, \code{M}, \code{alpha},
\code{d}, \code{ar_max}, \code{band}, \code{whiten_exclusion}, \code{whiten_guard}).}
}
\value{
A \code{seas_sa} object; see \code{\link[=seas_ssi]{seas_ssi()}}.
}
\description{
Convenience wrapper equal to \code{seas_ssi(seas_test(x, ...), phase_rule = phase_rule, donor_quantile = donor_quantile)}. It runs the full detect ->
specify -> identify -> operate workflow in a single call.

\strong{Default asymmetry (read this).} At this wrapper layer \code{phase_rule}
defaults to \code{"minimum"} (the filter-world / deconvolution rule) so that the
one-call path is usable out of the box, and the choice is echoed in every
print/summary tagged \verb{[declared identification]}. The lower-level
\code{\link[=seas_ssi]{seas_ssi()}} deliberately has \emph{no} default and forces you to declare
the rule, because filter-world versus components-world is an identification
commitment that is not testable at second order -- choosing it is choosing
which counterfactual you claim to recover. Pass \code{phase_rule = "zero"} for the
components-world (Wiener-shrinkage, classic-SSI) rule.
}
\examples{
set.seed(1)
n <- 120L; Phi <- 0.8
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
x <- stats::ts(z[401:(n + 400)], frequency = 4)

adj_min  <- seas_adjust(x)                      # default: minimum phase
adj_zero <- seas_adjust(x, phase_rule = "zero") # components-world rule

}
