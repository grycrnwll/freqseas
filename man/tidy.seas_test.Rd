% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-broom.R
\name{tidy.seas_test}
\alias{tidy.seas_test}
\title{Tidy a \code{seas_test} object}
\usage{
\method{tidy}{seas_test}(x, ...)
}
\arguments{
\item{x}{A \code{seas_test} object.}

\item{...}{Ignored.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}. Without a harmonic table: one row with
columns \code{statistic}, \code{p.value}, \code{spec}, \code{M}. With a harmonic table: one
row per harmonic, columns as provided by \code{spec$harmonic_table}.
}
\description{
One row per harmonic when \code{object$spec$harmonic_table} is present
(rendered as-is via \code{\link[tibble:as_tibble]{tibble::as_tibble()}}); otherwise a single summary row
with the detection statistic, p-value, specification label, and bin count.
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
tidy(tst)

}
