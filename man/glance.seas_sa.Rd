% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-broom.R
\name{glance.seas_sa}
\alias{glance.seas_sa}
\title{Glance at a \code{seas_sa} object}
\usage{
\method{glance}{seas_sa}(x, ...)
}
\arguments{
\item{x}{A \code{seas_sa} object.}

\item{...}{Ignored.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{decision}, \code{p.value},
\code{spec}, \code{M}, \code{d}, \code{ar_order} (as \code{\link[=glance.seas_test]{glance.seas_test()}}), plus
\code{phase_rule}, \code{post_p}, \code{max_gain}.
}
\description{
A one-row model-level summary: everything \code{\link[=glance.seas_test]{glance.seas_test()}} reports for
the embedded test, plus the declared phase rule, the post-adjustment
detection p-value (the self-check), and the maximum gain applied.
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
adj <- seas_ssi(tst, phase_rule = "minimum")
glance(adj)

}
