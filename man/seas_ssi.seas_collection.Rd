% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{seas_ssi.seas_collection}
\alias{seas_ssi.seas_collection}
\title{Adjust a \code{seas_collection}, mapping over keys}
\usage{
\method{seas_ssi}{seas_collection}(object, phase_rule, ...)
}
\arguments{
\item{object}{A \code{seas_collection} of type \code{"test"}.}

\item{phase_rule}{\code{"minimum"} or \code{"zero"}; required, no default (see
\code{seas_ssi()}).}

\item{...}{Passed on to \code{seas_ssi()} for each row's fit.}
}
\value{
A \code{seas_collection} of type \code{"sa"}, one \code{seas_sa} per key.
}
\description{
The \code{seas_collection} method for \code{seas_ssi()}: maps \code{seas_ssi()} over
each row's \code{fit} (a \code{seas_test}), producing a \code{seas_sa} per key. As with
the single-series \code{seas_ssi()}, \code{phase_rule} has no default and must be
supplied -- this method does not validate it itself; a missing or invalid
\code{phase_rule} surfaces as whatever error the per-series \code{seas_ssi()}
(\code{seas_ssi.seas_test()}) raises. Each row's \code{adjusted}/\code{seasonal} is then
re-clothed into the per-key tsibble via \code{fs_reclothe_sa()} (see the design
note at the top of this file).
}
\examples{
\dontrun{
tst <- seas_test(some_keyed_tsibble)
seas_ssi(tst, phase_rule = "minimum")
}

}
