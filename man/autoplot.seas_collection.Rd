% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{autoplot.seas_collection}
\alias{autoplot.seas_collection}
\title{Plot a \code{seas_collection} object with ggplot2}
\usage{
\method{autoplot}{seas_collection}(object, ..., max_keys = 12L)
}
\arguments{
\item{object}{A \code{seas_collection}.}

\item{...}{Ignored.}

\item{max_keys}{Maximum number of keys to plot. Default \code{12L}.}
}
\value{
A \code{ggplot} object.
}
\description{
The ggplot2 equivalent of \code{\link[=plot.seas_collection]{plot.seas_collection()}}: one facet per key
(\code{ggplot2::facet_wrap()}), the same per-key content as \code{\link[=plot.seas_sa]{plot.seas_sa()}}'s
\code{"series"} type (for a \code{type = "sa"} collection) or \code{\link[=plot.seas_test]{plot.seas_test()}}'s
periodogram (for a \code{type = "test"} collection). At most \code{max_keys} keys
are plotted; if \code{object} has more, a warning is issued and only the first
\code{max_keys} (in \code{object}'s row order) are shown. Requires the ggplot2
package (Suggests).
}
\examples{
\dontrun{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::autoplot(seas_test(some_keyed_tsibble))
}
}

}
