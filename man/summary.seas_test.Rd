% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-summary.R
\name{summary.seas_test}
\alias{summary.seas_test}
\title{Summarize a \code{seas_test} object}
\usage{
\method{summary}{seas_test}(object, ...)
}
\arguments{
\item{object}{A \code{seas_test} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"summary.seas_test"}, invisibly: a list with
fields \code{decision}, \code{p}, \code{alpha}, \code{evt} (\code{statistic}, \code{p}, \code{critical},
\code{N1}, \code{N0}), \code{spec} (\code{label}, \code{shoulder_p}, \code{phase_R}, \code{note}), \code{N},
\code{P}, \code{M}, \code{M_selection}, \code{whitener} (\code{d}, \code{d_reason}, \code{p}, \code{ar},
\code{bic_path}, \code{sigma2}), \code{harmonic_table} (\code{NULL} if not present on
\code{object}), \code{donor_pool_size}, \code{n_e}, and \code{call}.
}
\description{
A richer summary than \code{\link[=print.seas_test]{print.seas_test()}}: the full detection record
(statistic, critical value, seasonal/nonseasonal ordinate counts), the
specification record (shoulder p, block-phase resultant), the whitener
record (differencing decision + reason, AR order/coefficients, the full
M0-Whittle BIC path), the \code{M} selection record, the donor pool size, and
-- if present -- the per-harmonic table (\code{x$spec$harmonic_table}, added by
a concurrently-developed module; silently omitted when absent).
}
\examples{
set.seed(2026)
n <- 120L; Phi <- 0.9
e <- stats::rnorm(n + 400)
z <- numeric(n + 400)
for (t in 6:(n + 400)) z[t] <- 0.5 * z[t - 1] + Phi * z[t - 4] -
  0.5 * Phi * z[t - 5] + e[t]
tst <- seas_test(z[401:(n + 400)], frequency = 4)
s <- summary(tst)
s

}
